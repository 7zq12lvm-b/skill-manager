# AGENTS.md

## Repository workflow

- After every code or configuration change, run `./build.sh` from the repository root.
- Do not consider a change complete until `./build.sh` finishes successfully.
- If the build cannot run because of a missing local prerequisite, report the exact blocker and the command output.
